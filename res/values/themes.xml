<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.RvcSettings" parent="@style/Theme.CarUi.WithToolbar" />

    <!-- The camera screen hosts the settings panel, which needs a car UI theme. -->
    <style name="Theme.RvcCamera" parent="@style/Theme.CarUi.NoToolbar">
        <item name="android:windowBackground">@android:color/black</item>
    </style>
</resources>
